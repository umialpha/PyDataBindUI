这是本人第一个和cocos2dx结合的MVVM框架。
因为本人使用的引擎将cocos2dx绑定了python。因此使用的是python。

cocos:
	对cocos的界面（.csd）读取。

editor:
	编辑器，读取csd数据，view数据，view-model数据，用以进行数据绑定，事件绑定等。

view:
	对cocos控件的一层封装。
	
view-model:
	用户可以将所有写的view-model写在该目录。
	
